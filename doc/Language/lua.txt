

                                                                         *lua*

#include <lua.h>
#include <lualib.h>
#include <luaxlib.h>


int main(){
    char buff[256];
    int error;
    lua_State* L = lua_open();
    luaopen_base(L);
    luaopen_table(L);
    
    errror = luaL_loadbuffer(L, buff, strlen(buff), "line");
    lua_pcall(L, 0, 0, 0);
}



                                                               *lua_functions*


    luaL_loadfile


vim:tw=78:ts=8:ft=help:norl:
